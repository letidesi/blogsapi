{
  "name": "backend-test",
  "version": "1.0.0",
  "description": "Você vai arquiteturar e desenvolver uma API de um CRUD de posts de blog. Começando pela API, você vai desenvolver alguns endpoints (seguindo os principios do REST) que estarão conectados ao seu banco de dados.",
  "main": "server.js",
  "scripts": {
    "test": "rm -rf .env && cat .env.test >> .env && npx jest --colors --detectOpenHandles --config=jest.config.js",
    "dev": "rm -rf .env && cat .env.dev >> .env && nodemon server.js"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/betrybe/backend-test.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/betrybe/backend-test/issues"
  },
  "homepage": "https://github.com/betrybe/backend-test#readme",
  "dependencies": {
    "bcrypt": "^5.0.1",
    "cors": "^2.8.5",
    "dotenv-safe": "^8.2.0",
    "express": "^4.17.2",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^6.1.7"
  },
  "devDependencies": {
    "jest": "^27.4.7",
    "nodemon": "^2.0.15"
  }
}
